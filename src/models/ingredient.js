class Ingredient {
  constructor(ingredientId, name, img) {
    //Set the object id to a uuid
    this.ingredientId = ingredientId;
    this.name = name;
    this.img = img;
    //this.output_of_step = output_of_step;
  }
}